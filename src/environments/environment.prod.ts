export const environment = {
  production: true,
  apiUrl: "http://angular2api1.azurewebsites.net/api/internships2" // not an actual api endpoint
// this is just to demonstrate enviroment variables.
};
